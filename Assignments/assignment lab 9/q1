#include <stdio.h>

 int product(int x, int y);

 int main(){
 	int x, y, result;
 	
 	printf("Enter two numbers:\n");
 	scanf("%d %d", &x, &y);
 	
	result= product(x, y);
	printf("the product of %d and %d is: %d", x, y, result);
	
	
	return 0;
}
 int product(int x, int y){
 	return x*y;
 }
