#include <stdio.h>
 int main() 
 {
	 float slope, a, b;
	 printf("first point : (5,4)\n" );
	 
	 printf("second point : (3,2)\n" );
	 
	 a=2-4;
	 b=3-5;
	 slope=a/b;
	 
	 printf("slope is : %f", slope);
	 
	 return 0;
 }